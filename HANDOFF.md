# HANDOFF — стан проєкту на вечір суботи 12.07

**Прочитай мене першим.** Потім: `README.md` (як усе запускати) → `PLAN.md` (гіпотези і навіщо) → `report/notes.md` (знахідки, вже готові формулювання для звіту).

## Де ми зараз (що ВЖЕ зроблено — не переробляти)

- ✅ Дані завантажені й перевірені, лежать у `data/raw` (закомічені в git).
- ✅ `01_data_preparation.qmd` виконаний повністю: **`data/processed/analysis_table.csv` готова** — 1230 рядків (маршрут × напрямок × слот), критерії курсу виконані.
- ✅ Методика headway провалідована на 5 маршрутах проти офіційних описів (4/5 повний збіг) — деталі та готові речення для звіту в `report/notes.md`.
- ✅ Збирач GPS крутиться на ноуті Арсена (бонус-частина), знімки падають у `data/raw/rt/`.
- ⏳ НЕ зроблено: довжина маршрутів (чанк `route-length` у 01 — порожній, не блокує тести), райони+населення для H4, весь `02_analysis.qmd`, звіт, слайди.

## Структура даних (analysis_table.csv)

Одиниця = маршрут × напрямок × часовий слот. Колонки: `route_id`, `direction_id`, `time_slot` (4 слоти), `peak` (TRUE/FALSE), `transport_type` (трамвай/тролейбус/автобус), `n_vehicles`, `total_capacity`, `n_ctrl_points`, `sched_headway_min` (головна залежна), `sd_headway_min`, `n_departures`. NA у vehicles — лише 3 тимчасові маршрути (1т/3т/4т).

## Хто що робить у неділю

**C (статистика) — `02_analysis.qmd`.** Датасет тебе чекає. Іди по чанках зверху вниз: EDA → H1 → H2 → H3 → H5 (+ bootstrap якщо встигаєш). Схема КОЖНОЇ гіпотези: діагностика (Shapiro/QQ) → вибір тесту → p-value → висновок словами. Тільки методи курсу, лекції 6–10 — жодних ANOVA/OLS (ми їх не вчили, деталі чому — в PLAN.md §3). Очікуй скошені розподіли → скоріш за все рангові тести.

**A (дані) — два завдання.** (1) Знайти GeoJSON меж районів Києва + чисельність населення по районах (портал data.kyivcity.gov.ua; fallback — OSM через пакет osmdata) → віддати B/C для H4. (2) Увечері: зупинити збирач, `python scripts/parse_realtime.py` (інструкція з venv у README §2.2) → `03_realtime_bonus.qmd`. Якщо бонус не йде за 3-4 години — дропаємо, це ок.

**B (Арсен, датасет).** Дороблю чанк `route-length` (shapes → st_length) і H4-агрегацію по районах, коли A принесе межі. На підхваті у C по питаннях датасету.

**D (звіт/слайди).** Каркас звіту в `report/` (Quarto): Introduction (мотивація + code book з таблиці вище + RQ і гіпотези з PLAN.md §3) + Methodology (болванка вже в notes.md §1 — можна переносити) + каркас Google Slides за структурою з PLAN.md §6 (тиждень 4). Discussion-заготовки — notes.md §2.

## Правила, щоб не зламати одне одному роботу

1. `git pull` ЗАВЖДИ перед початком, push — щойно щось працює. Кожен працює у своєму .qmd — конфліктів не буде.
2. `data/raw` не чіпати руками. Чанк download у 01 НЕ запускати (там попередження — портал редіректить на http, і стандартний завантажувач падає).
3. Після рестарту R виконуй чанки зверху вниз (вони ділять сесію), Cmd+Option+P = Run All Chunks Above.
4. Кожне рішення («чому такий фільтр», «чому цей тест») — одразу в `report/notes.md` §3: з цього будуються Methodology і відповіді на Q&A.
5. Рубрика вимагає, щоб КОЖЕН розумів усе: якщо щось незрозуміло в чужій частині — питай зараз, не на захисті.

## Синк

Неділя, [ЧАС ВСТАВИТИ], 30 хв: C показує результати тестів, D — каркас звіту, A — райони/бонус. Після синку: збірка звіту → слайди → репетиція №1 з таймером (≤7 хв). Понеділок зранку: репетиція №2, кожен окремо здає ідентичний пакет (звіт + код + слайди) у Moodle.

## Довідка

- Google Doc (дзеркало нотаток): https://docs.google.com/document/d/1iylZOw8VUtdyl2QOTUx6g67HaQEbs-_QZaYXVzfLhZ0/edit
- Джерело даних: https://data.kyivcity.gov.ua/organization/komunal-ne-pidpryiemstvo-kyivpastrans (прямі лінки — PLAN.md §2)
- Academic integrity: AI-код дозволений з цитуванням (уже вписано в README §4); текст звіту пишемо самі.
